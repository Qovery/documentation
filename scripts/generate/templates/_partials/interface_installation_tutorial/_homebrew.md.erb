<Steps headingDepth={<%= heading_depth.to_json %>}>
<ol>
<li>

### Add the Timber tap and install `qovery`

```bash
brew tap timberio/brew && brew install qovery
```

[Looking for a specific version?][docs.package_managers.homebrew]

</li>
<li>

### Configure Qovery

<ConfigExample
  format="toml"
  path={<%= interface.config_path.to_json %>}
  sourceName={<%= (source && source.name).to_json %>}
  sinkName={<%= (sink && sink.name).to_json %>} />

</li>
<li>

### Start Qovery

<%= interface_start(interface, requirements: (source && source.requirements)) %>

</li>
</ol>
</Steps>

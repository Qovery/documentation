# STDIO Example
# ------------------------------------------------------------------------------
# A simple STDIN / STDOUT example. This script is used in the getting started
# guide:
#
# https://qovery.dev/guides/getting-started

[sources.in]
    type = "stdin"

[sinks.out]
    inputs = ["in"]
    type = "console"
    encoding = "text"

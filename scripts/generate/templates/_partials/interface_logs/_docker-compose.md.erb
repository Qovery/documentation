If you started Qovery through Docker compose you can use the following command
to access Qovery's logs:

```bash
docker-compose logs -f qovery
```

Replace `qovery` with the name of Qovery's service if it is not called `qovery`.

```bash
qovery --config <%= interface.config_path %>
```

<CodeExplanation>

* `qovery` is placed in your `$PATH`.
* You must create a [Qovery configuration file][docs.configuration] to
  successfully start Qovery.

</CodeExplanation>
